# === Python / Virtual Environment ===
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.python-version

# === Editors / IDEs ===
.vscode/
.idea/

# === OS cruft ===
.DS_Store
Thumbs.db
._*

# === Temp working directories ===
tmp/
.tmp_doc_md_files*
*.tmp
*.tmpout/

# === Logs ===
*.log
logs/

# === Local environment & secrets ===
.env
.env.*
tools/client_secret.json
tools/token.json
tools/secrets/*
!tools/secrets/.gitkeep

# === Downloaded media (never track in Git) ===
# Keep the quarantine README so policy is visible in-tree.
downloads/
!downloads/_quarantine/
!downloads/_quarantine/README.md

# Common media extensions (in case any slip outside downloads/)
*.mp4
*.mp3
*.m4a
*.webm
*.opus
*.wav
*.flac
*.mkv
*.mov
*.avi

# yt-dlp partials and temp files
*.part
*.ytdl
*.temp
*.aria2
*.info.json

# Audacity project files (large, local only)
*.aup3
downloads/audio/working/

# === Build artifacts ===
# We publish the site from /docs/, so all of /build/ can be ignored.
build/
index*.bak.json
out/
dist/

# === Private backups & bundles (local only) ===
backups/
private_backups/
bundle-v2.3-media/
chris-bache-archive.mirror/

# === Reports (local scans / scratch) ===
reports/
reports/quote_packs/

# === Fixity and checksum archives (keep live checksums; ignore archive cache) ===
checksums/_archive/

# === Vectors (default ignore; allow-list the canonical pair) ===
vectors/*
!vectors/.gitkeep
!vectors/bache-talks.embeddings.parquet
!vectors/bache-talks.index.faiss

# === Patch pipeline ===
# Keep inputs/work/outputs tracked if they’re source-of-truth; drop logs noise.
patches/**/logs/

# === Images / thumbnails ===
# Do NOT globally ignore images; site assets in docs/ must be tracked.
# If you want to ignore image dumps, place them under downloads/ or build/.
# (Intentionally no *.jpg/*.png ignore here.)

# === Archives / release bundles ===
*.zip
*.tar
*.tar.gz
*.tgz

# === Misc old/back files ===
*.bak
*.old
# scratch files
/tmp_one.txt
# never track generated HTML under transcript subtrees we don't publish
/sources/transcripts/_archive/*.html
/sources/transcripts/_archive/**/*.html
/sources/transcripts/extras/*.html
/sources/transcripts/extras/**/*.html
