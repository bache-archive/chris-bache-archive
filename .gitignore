--- a/.gitignore
+++ b/.gitignore
@@ -1,3 +1,4 @@
+# === Python / Virtual Environment ===
 .venv/
 __pycache__/
 *.pyc
 
@@ -10,6 +11,7 @@
 tmp/
 .tmp_doc_md_files*
 
+# === Downloaded media (never track in Git) ===
 downloads/
 *.mp4
 *.mp3
@@ -31,6 +33,7 @@
 *.zip
 
 # === Images / thumbnails ===
+# (keep source images elsewhere if needed; site thumbs should be build artifacts)
 *.jpg
 *.jpeg
 *.png
@@ -38,8 +41,11 @@
 # === Local environment & secrets ===
 .env
 .env.local
-tools/client_secret.json
-tools/token.json
+tools/client_secret.json
+tools/token.json
+# keep secrets dir ignored but allow a placeholder
+tools/secrets/*
+!tools/secrets/.gitkeep
 
 # === Audacity project files (large, local only) ===
 downloads/audio/working/
@@ -47,44 +53,45 @@
 
 # === Build artifacts (skip local builds, keep public docs) ===
 build/
 index*.bak.json
 
 # === Private backups & reports ===
 private_backups/
 reports/
 
 # === Fixity and checksum archives ===
 checksums/_archive/
 
-# === Allowlist: keep these large but necessary RAG assets tracked ===
-!vectors/
-!vectors/bache-talks.embeddings.parquet
-!vectors/bache-talks.index.faiss
-
-# Ignore everything else under vectors by default
-vectors/*
+# === Vectors (ignore all by default, allowlist key assets) ===
+vectors/*
+!vectors/.gitkeep
+!vectors/bache-talks.embeddings.parquet
+!vectors/bache-talks.index.faiss
 
 
 dist/
 docs/educational/
 reports/quote_packs/
 backups/sources_json/
 
-dist/
-
-downloads/
-
-private_backups/
-
-bundle-v2.3-media/
-
-backups/sources_json/
-
-reports/quote_packs/
-
-alignments/lsdmu/
-
-build/site/docs/
-chris-bache-archive.mirror/
-build/site/docs/educational/
+# (deduped repeated lines)
+bundle-v2.3-media/
+alignments/lsdmu/
+chris-bache-archive.mirror/
+
+# === Patch pipeline ===
+# keep patch logs out of git; inputs/work/outputs are tracked
+patches/**/logs/
+
+# === Notebooks & misc local noise ===
+.ipynb_checkpoints/
+*.bak
+*.old
+*.tmp